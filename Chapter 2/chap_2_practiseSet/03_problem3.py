
f = input("Enter the number")

print(type(f)) 

#The type of f variable will be always <class str> becoz input is always taken in string format, so we need to convert it to int, float which ever we want we can doit